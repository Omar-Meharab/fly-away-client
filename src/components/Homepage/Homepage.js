import React from 'react';
import './Homepage.css';
import Navbar from '../Navbar/Navbar';

const Homepage = () => {
    return (
        <div>
            <Navbar></Navbar>
        </div>
    );
};

export default Homepage;